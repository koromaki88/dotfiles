# dotfiles
Self-explanatory

Basic info:
- Arch
- AwesomeWM/X11
- bash
- Firefox
- Alacritty
- Neovim

For SDDM theme `sugar-candy` move into `/usr/share/sddm/themes/` and change `[Theme]` in `/etc/sddm.conf`
